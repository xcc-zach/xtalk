{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main XTalk window to obtain and stop its managed backend.",
  "windows": [
    "main"
  ],
  "permissions": [
    "allow-get-backend-connection",
    "allow-shutdown-backend"
  ]
}
