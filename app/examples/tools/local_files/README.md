# 本地文件浏览器

在 XTalk 的“设置与诊断”中选择当前目录，然后点击“应用并重启本地服务”。

工具名为 `browse_local_files`：

- 传入目录时，列出该目录的直接子项；
- 传入文件时，返回其 UTF-8 文本内容；
- 支持 `~`、相对路径和绝对路径；
- 不限制可访问的根目录，也不使用权限配置。

`max_entries` 和 `max_characters` 仅用于控制单次工具结果的长度。
